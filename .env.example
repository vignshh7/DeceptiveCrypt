# Copy this file to .env and change values as needed.
# (Exam-safe) demo values only.

# Browser demo secret key (correct key in UI)
DEMO_SECRET_KEY=demo-secret

# Optional: original file-monitor demo secrets
CRYPTO_SYSTEM_PASSPHRASE=demo-user-passphrase
CRYPTO_ATTACKER_PASSPHRASE=demo-attacker-pass

# Optional server bind
DEMO_HOST=127.0.0.1
DEMO_PORT=8000
